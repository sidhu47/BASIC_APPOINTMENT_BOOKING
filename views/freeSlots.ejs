<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <!-- CSS only -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.1/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-+0n0xVW2eSR5OomGNYDnhzAbDsOXxcvSN1TPprVMTNDbiYZCxYbOOl7+AMvyTG2x" crossorigin="anonymous">
    <title>Free Slots</title>
</head>
<body>
    <div class="container">
        <h1 class="mb-4"> These are the free slots  </h1> 

        <% slots.forEach(value => { %>
            <div class="card mb-1">
                <div class="card-body">
                    <% let temp = value %>
                    <% temp = temp.slice(11, 16) %>
                    <h4 class="card-title mb-2"><%= temp %></h4>
                    <div class="card-subtitle text-muted mb-2">
                        <%= value %>
                    </div>
                </div>
            </div>
        <% }) %>
        
        <form action="/events" method="POST" class="mt-4">
            <div class="form-group mb-4">
                <label  for="name" class="mb-2">Enter your name</label>
                <input required type="text" name="name" id="name" class="form-control">
            </div>

            <div class="form-group mb-4">
                <label  for="date" class="mb-2">Enter your dateTime is ISO(Copy the ISO string from above)</label>
                <input required type="text" name="date" id="date" class="form-control">
            </div>
            
            <div class="form-group mb-4">
                <label  for="duration" class="mb-2">Enter your duration</label>
                <input required type="number" name="duration" id="duration" class="form-control">
            </div>

            <button type="submit" class="btn btn-primary">Book Appointment</button>
        </form>
        
        

        
    </div>
</body>
</html>